import React from "react";
import style from "./Project.module.css";
type Props = {};

const ProjectView = (props: Props) => {
  return <div className={style["project-container"]}>ProjectView</div>;
};

export default ProjectView;
